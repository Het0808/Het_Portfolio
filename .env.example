# Optional: enable live LLM answers in the AI Assistant.
# Without this key the assistant falls back to fast, built-in predefined answers.
OPENAI_API_KEY=

# Optional: override the model used by the assistant route.
OPENAI_MODEL=gpt-4o-mini
